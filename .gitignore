node_modules/
.next/
dist/
*.tsbuildinfo

# Secrets never land in this repository (CLAUDE.md). `.env.example` is the only
# environment file that is ever committed, and every value in it is empty.
.env
.env.*
!.env.example

# Runtime data — image cache, uploads, stockpile binaries (plan §6.6.1 keeps the
# manifest in git and the images out of it). Exception: the CURATED login-photo
# set (5 reviewed, licensed webps, ~540 KB total) IS committed — it must exist
# at image-build time for the standalone bundle, and hand-reviewed licensing is
# exactly what belongs in a diff. The §6.6.1 stockpile (~250 images) stays out.
/media/
packages/stockpile/images/
.photo-review/

*.log
.DS_Store

# scratch
.audit-tmp/
